name: Legacy Phase 29 Browser Request Integrity (Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: vp3-legacy-phase29-${{ github.ref }}
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Retired workflow notice
        run: echo "Phase 29 is retained inside the current Phase 30 and VP3 retained certification workflows."
